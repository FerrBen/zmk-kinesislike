# EC11 rotary encoder support (right side only)
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y

# Use polling for direct GPIO kscan (EC11 push button)
# Interrupt mode may not work for this pin
CONFIG_ZMK_KSCAN_DIRECT_POLLING=y
